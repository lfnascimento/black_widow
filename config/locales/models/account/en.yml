account_info: &account_info
  errors:
    models:
      account:
        attributes:
          base:
            initial_balance_not_eq_balance: Initial Balance cannot be different from Balance
en:
  activemodel:
    <<: *account_info
  activerecord:
    <<: *account_info
